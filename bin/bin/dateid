#!/bin/sh

date_id="$1"

seconds_in_day="$((24 * 60 * 60))"
tenzing_epoch_s="$(date --date '2000-01-01' +%s)"
date_epoch_s="$((tenzing_epoch_s + date_id * seconds_in_day))"
date --date="@${date_epoch_s}" +%F

